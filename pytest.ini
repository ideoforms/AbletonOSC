[pytest]
# Pytest configuration for AbletonOSC

# Only run tests from the tests/ directory
testpaths = tests

# Exclude development scripts directory
norecursedirs = devel .git __pycache__ *.egg-info

# Verbose output
addopts = -v
